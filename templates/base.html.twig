<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="robots" content="index, follow">
        <meta name="description" content="{% block meta_description %}Duo Import MDG - Votre partenaire de confiance pour l'importation de produits depuis la Chine. Nous vous accompagnons dans toutes les étapes de votre projet d'importation.{% endblock %}">
        <meta name="keywords" content="{% block meta_keywords %}importation, Chine, Madagascar, sourcing, logistique, fournisseurs, contrôle qualité{% endblock %}">
        <meta name="author" content="Duo Import MDG">
        <title>{% block title %}Duo Import MDG{% endblock %}</title>
        <link rel="icon" href="favicon.ico">
        
        <!-- Script d'optimisation du LCP -->
        <script>
            // Fonction pour précharger l'image hero
            (function() {
                var heroImage = new Image();
                heroImage.src = "{{ asset('images/hero-image-300.webp') }}";
                heroImage.fetchPriority = "high";
                heroImage.onload = function() {
                    var heroImages = document.querySelectorAll('.hero-image img');
                    if (heroImages.length > 0) {
                        heroImages.forEach(function(img) {
                            img.classList.add('loaded');
                        });
                    }
                };
            })();
        </script>
        
        <!-- Préchargement des polices pour éviter les décalages de mise en page -->
        <link rel="preload" href="{{ asset('css/fonts.min.css') }}" as="style">
        <link rel="preload" href="https://fonts.gstatic.com/s/poppins/v20/pxiEyp8kv8JHgFVrJJfecg.woff2" as="font" type="font/woff2" crossorigin>
        <link rel="preload" href="https://fonts.gstatic.com/s/poppins/v20/pxiByp8kv8JHgFVrLGT9Z1xlFQ.woff2" as="font" type="font/woff2" crossorigin>
        <link rel="preload" href="https://fonts.gstatic.com/s/poppins/v20/pxiByp8kv8JHgFVrLEj6Z1xlFQ.woff2" as="font" type="font/woff2" crossorigin>
        <link rel="preload" href="https://fonts.gstatic.com/s/poppins/v20/pxiByp8kv8JHgFVrLCz7Z1xlFQ.woff2" as="font" type="font/woff2" crossorigin>
        
        <!-- Préchargement des ressources critiques avec dimensions explicites -->
        <link rel="preload" href="{{ asset('images/logo.webp') }}" as="image" fetchpriority="high" imagesizes="40px" imagesrcset="{{ asset('images/logo.webp') }} 40w">
        <link rel="preload" href="{{ asset('images/hero-image-300.webp') }}" as="image" fetchpriority="high" imagesizes="300px" imagesrcset="{{ asset('images/hero-image-300.webp') }} 300w">
        
        <!-- Chargement des polices avec font-display: swap -->
        <link rel="stylesheet" href="{{ asset('css/fonts.min.css') }}">
        
        <!-- CSS critique en ligne pour un rendu rapide -->
        <style>
            /* Styles critiques pour le rendu initial */
            :root {
                --primary-color: #2b4eff;
                --primary-dark: #1a3dff;
                --secondary-color: #6c757d;
                --text-color: #444444;
                --navbar-height: 80px;
            }

            body {
                font-family: 'Poppins', 'Poppins Fallback', sans-serif;
                color: var(--text-color);
                line-height: 1.7;
                margin: 0;
                overflow-x: hidden;
            }

            /* Réserver l'espace pour les éléments qui vont charger */
            .navbar {
                padding: 0;
                background: #fff !important;
                box-shadow: 0 2px 15px rgba(0, 0, 0, 0.05);
                height: var(--navbar-height);
                display: flex;
                align-items: center;
            }

            .navbar-brand {
                font-weight: 700;
                font-size: 1.5rem;
                display: flex;
                align-items: center;
                height: var(--navbar-height);
            }

            .navbar-brand img {
                margin-right: 10px;
                width: 40px;
                height: 40px;
            }

            .user-icon-active {
                color: #0d6efd;
            }

            .user-icon-inactive {
                color: #6c757d;
            }

            /* Styles minimaux pour le contenu principal */
            main {
                min-height: 300px;
            }

            /* Styles minimaux pour le footer */
            .footer-area {
                background: #1a1a1a;
                color: #fff;
                padding: 80px 0 30px;
            }
            
            /* Styles minimaux pour la section hero */
            .hero-section {
                background: linear-gradient(45deg, #2b4eff, #1a3dff);
                padding: 100px 0;
                position: relative;
                overflow: hidden;
                min-height: 500px;
            }
            
            .hero-section h1 {
                color: #fff;
                font-weight: 700;
            }
            
            .hero-section p {
                color: rgba(255, 255, 255, 0.9);
            }
            
            /* Réserver l'espace pour l'image hero */
            .hero-image {
                aspect-ratio: 3/2;
                width: 100%;
                max-width: 600px;
                max-height: 400px;
                margin: 0 auto;
                position: relative;
                overflow: hidden;
                background-color: #1a3dff;
                border-radius: 0.5rem;
                box-shadow: 0 1rem 3rem rgba(0,0,0,.175);
            }
            
            .hero-image img {
                width: 100%;
                height: auto;
                max-width: 600px;
                max-height: 400px;
                object-fit: cover;
                opacity: 0;
                transition: opacity 0.3s ease;
            }
            
            .hero-image img.loaded {
                opacity: 1;
            }
            
            /* Optimisation pour le LCP */
            @media (max-width: 768px) {
                .hero-section {
                    padding: 60px 0;
                    min-height: auto;
                }
                
                .hero-image {
                    max-width: 100%;
                    max-height: 300px;
                }
            }
            
            /* Styles pour éviter les décalages de mise en page */
            h1, h2, h3, h4, h5, h6 {
                font-weight: 700;
            }
            
            .display-4 {
                font-size: 2.5rem;
                line-height: 1.2;
            }
            
            .lead {
                font-size: 1.25rem;
                font-weight: 400;
            }
            
            /* Styles pour les boutons */
            .btn {
                font-weight: 500;
                padding: 0.5rem 1.5rem;
            }
            
            /* Styles pour la navigation */
            .navbar-nav {
                margin-left: auto;
                height: 100%;
            }
            
            .nav-link {
                padding: 0.5rem 1rem;
                font-weight: 500;
                height: 100%;
                display: flex;
                align-items: center;
            }
            
            /* Navbar toggle button */
            .navbar-toggler {
                border: none;
                padding: 0.5rem;
                width: 44px;
                height: 44px;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            
            .navbar-toggler-icon {
                width: 24px;
                height: 24px;
            }
            
            /* Navbar collapse */
            #navbarNav {
                transition: height 0.3s ease;
            }
            
            /* Fix for mobile navbar */
            @media (max-width: 991px) {
                .navbar-collapse {
                    position: absolute;
                    top: var(--navbar-height);
                    left: 0;
                    right: 0;
                    background: white;
                    box-shadow: 0 5px 10px rgba(0,0,0,0.1);
                    z-index: 1000;
                    padding: 1rem;
                    height: auto;
                    max-height: calc(100vh - var(--navbar-height));
                    overflow-y: auto;
                }
            }
            
            /* Styles pour éviter les décalages dans le footer */
            .footer-widget h5 {
                font-size: 1.25rem;
                margin-bottom: 1.5rem;
                color: #fff;
            }
            
            .footer-links {
                padding-left: 0;
            }
            
            .footer-links li {
                margin-bottom: 0.5rem;
            }
        </style>
        
        <!-- Chargement asynchrone des styles non critiques -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" media="print" onload="this.media='all'">
        <link rel="stylesheet" href="{{ asset('css/styles.min.css') }}" media="print" onload="this.media='all'">
        <link rel="stylesheet" href="{{ asset('css/parallax.min.css') }}" media="print" onload="this.media='all'">
        
        <noscript>
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('css/styles.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/parallax.min.css') }}">
        </noscript>
        
        {% block stylesheets %}{% endblock %}
        
        <!-- Google tag (gtag.js) - déplacé en bas et chargé de manière asynchrone -->
        <script async defer src="https://www.googletagmanager.com/gtag/js?id=G-5WQD09147M"></script>
        <script>
            window.addEventListener('load', function() {
                window.dataLayer = window.dataLayer || [];
                function gtag(){dataLayer.push(arguments);}
                gtag('js', new Date());
                gtag('config', 'G-5WQD09147M');
            });
        </script>
    </head>
    <body>
        <!-- Header -->
        <header class="header-area">
            <nav class="navbar navbar-expand-lg navbar-light">
                <div class="container">
                    <a class="navbar-brand" href="{{ path('app_home') }}">
                        <img src="{{ asset('images/logo.webp') }}" alt="Logo" height="40" width="40" class="d-inline-block align-text-top" fetchpriority="high">
                        Duo Import MDG
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'app_services' %}active{% endif %}" href="{{ path('app_services') }}">Nos Services</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'app_about' %}active{% endif %}" href="{{ path('app_about') }}">Qui sommes-nous</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') == 'app_contact' %}active{% endif %}" href="{{ path('app_contact') }}">Contact</a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-primary {% if app.request.get('_route') == 'app_quote' %}active{% endif %}" href="{{ path('app_quote') }}">Demander un devis</a>
                            </li>
                            {% if app.user %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="fas fa-user user-icon-active me-1"></i> {{ app.user.firstName }}
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="{{ path('app_user_profile') }}">Mon profil</a></li>
                                        <li><a class="dropdown-item" href="{{ path('app_user_quotes') }}">Mes devis</a></li>
                                        {% if is_granted('ROLE_ADMIN') %}
                                            <li><hr class="dropdown-divider"></li>
                                            <li><a class="dropdown-item" href="{{ path('app_quote_dashboard') }}">Tableau de bord</a></li>
                                        {% endif %}
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
                                    </ul>
                                </li>
                            {% else %}
                                <li class="nav-item dropdown">
                                    <a class="nav-link" href="{{ path('app_login') }}" id="navbarUserIcon" aria-label="Connexion à votre compte">
                                        <i class="fas fa-user user-icon-inactive fs-5" aria-hidden="true"></i>
                                        <span class="visually-hidden">Connexion</span>
                                    </a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>

        <!-- Main Content -->
        <main>
            {% block body %}{% endblock %}
        </main>

        <!-- Footer -->
        <footer class="footer-area">
            <div class="container">
                <div class="row">
                    <div class="col-lg-4 col-md-6 mb-4 mb-md-0">
                        <div class="footer-widget">
                            <h5>À propos de nous</h5>
                            <p>Duo Import MDG est votre partenaire de confiance pour l'importation de produits depuis la Chine. Nous vous accompagnons dans toutes les étapes de votre projet d'importation.</p>
                            <div class="social-icons mt-4">
                                <a href="https://www.facebook.com/duoimportmdg" target="_blank" aria-label="Suivez-nous sur Facebook">
                                    <i class="fab fa-facebook-f" aria-hidden="true"></i>
                                    <span class="visually-hidden">Facebook</span>
                                </a>
                                <a href="#" target="_blank" aria-label="Suivez-nous sur TikTok">
                                    <i class="fab fa-tiktok" aria-hidden="true"></i>
                                    <span class="visually-hidden">TikTok</span>
                                </a>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-4 col-md-6 mb-4 mb-md-0">
                        <div class="footer-widget">
                            <h5>Liens rapides</h5>
                            <ul class="footer-links list-unstyled">
                                <li><a href="{{ path('app_home') }}">Accueil</a></li>
                                <li><a href="{{ path('app_services') }}">Nos Services</a></li>
                                <li><a href="{{ path('app_about') }}">Qui sommes-nous</a></li>
                                <li><a href="{{ path('app_contact') }}">Contact</a></li>
                                <li><a href="{{ path('app_quote') }}">Demander un devis</a></li>
                                {% if not app.user %}
                                    <li><a href="{{ path('app_login') }}">Connexion</a></li>
                                    <li><a href="{{ path('app_user_register') }}">Créer un compte</a></li>
                                {% else %}
                                    <li><a href="{{ path('app_user_profile') }}">Mon profil</a></li>
                                    <li><a href="{{ path('app_user_quotes') }}">Mes devis</a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                    <div class="col-lg-4 col-md-6">
                        <div class="footer-widget">
                            <h5>Contactez-nous</h5>
                            <ul class="footer-contact list-unstyled">
                                <li>
                                    <i class="fas fa-map-marker-alt me-2"></i>
                                    Ankadindravola Ivato<br>Antananarivo - Madagascar
                                </li>
                                <li>
                                    <i class="fas fa-phone me-2"></i>
                                    <a href="tel:+261384271168">+261 38 42 711 68</a>
                                </li>
                                <li>
                                    <i class="fas fa-envelope me-2"></i>
                                    <a href="mailto:contact@duoimport.mg">contact@duoimport.mg</a>
                                </li>
                                <li>
                                    <i class="fas fa-clock me-2"></i>
                                    Lun - Ven: 8h00 - 17h00
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
                <hr class="mt-5">
                <div class="row align-items-center">
                    <div class="col-md-6">
                        <p class="mb-0">&copy; {{ "now"|date("Y") }} Duo Import MDG. Tous droits réservés.</p>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <p class="mb-0">
                            <a href="{{ path('app_cgv') }}" class="me-3">CGV</a>
                            <a href="#">Politique de confidentialité</a>
                        </p>
                    </div>
                </div>
            </div>
        </footer>

        <!-- Back to Top -->
        <a href="#" class="back-to-top d-flex align-items-center justify-content-center" aria-label="Retour en haut de la page">
            <i class="fas fa-arrow-up" aria-hidden="true"></i>
            <span class="visually-hidden">Retour en haut</span>
        </a>

        <!-- Chargement différé des ressources non critiques -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" media="print" onload="this.media='all'">
        <noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"></noscript>

        <!-- Scripts -->
        <script src="{{ asset('js/image-optimizer.min.js') }}" defer></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
        <script src="{{ asset('js/scripts.min.js') }}" defer></script>
        <script src="{{ asset('js/parallax.min.js') }}" defer></script>
        {% block javascripts %}{% endblock %}
        
        <script>
            // Back to top button
            document.addEventListener('DOMContentLoaded', function() {
                const backToTop = document.querySelector('.back-to-top');
                if (backToTop) {
                    window.addEventListener('scroll', () => {
                        if (window.pageYOffset > 100) {
                            backToTop.classList.add('active');
                        } else {
                            backToTop.classList.remove('active');
                        }
                    });
                }

                // Animation on scroll
                const fadeElements = document.querySelectorAll('.fade-up');
                
                if (fadeElements.length > 0) {
                    const observer = new IntersectionObserver((entries) => {
                        entries.forEach(entry => {
                            if (entry.isIntersecting) {
                                entry.target.classList.add('active');
                            }
                        });
                    }, { threshold: 0.1 });

                    fadeElements.forEach(element => {
                        observer.observe(element);
                    });
                }
            });
        </script>
    </body>
</html> 