<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouveau message de contact</title>
</head>
<body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333;">
    <div style="max-width: 600px; margin: 0 auto; padding: 20px;">
        <h1 style="color: #2c3e50;">Nouveau message de contact</h1>
        
        <div style="background: #f8f9fa; padding: 20px; border-radius: 5px; margin: 20px 0;">
            <h2 style="color: #2c3e50; margin-top: 0;">Informations de l'expéditeur</h2>
            <p><strong>Nom :</strong> {{ contact.name }}</p>
            <p><strong>Email :</strong> {{ contact.email }}</p>
            <p><strong>Sujet :</strong> {{ contact.subject }}</p>
        </div>

        <div style="background: #f8f9fa; padding: 20px; border-radius: 5px;">
            <h2 style="color: #2c3e50; margin-top: 0;">Message</h2>
            <p style="white-space: pre-wrap; background: #fff; padding: 10px; border-radius: 3px;">{{ contact.message }}</p>
        </div>

        <p style="color: #666; font-size: 0.9em; margin-top: 20px; text-align: center;">
            Ce message a été envoyé le {{ contact.createdAt|date('d/m/Y à H:i') }}
        </p>
    </div>
</body>
</html> 